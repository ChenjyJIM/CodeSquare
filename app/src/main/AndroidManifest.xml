<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a54545.phpconnectnew">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AdminActivity" />
        <activity android:name=".OrderShow" />
        <activity android:name=".OrderActivity" />
        <activity android:name=".DataActivity" />
        <activity android:name=".ShowCustomerActivity" />
        <activity android:name=".ShowAmountActivity" />
        <activity android:name=".CustomerActivity" />
        <activity
            android:name=".UserActivity"
            android:label="@string/title_activity_user"
            android:theme="@style/AppTheme" />
        <activity android:name=".test" />
        <activity android:name=".ShowProduct" />
        <activity android:name=".WifiActivity"></activity>
    </application>

</manifest>